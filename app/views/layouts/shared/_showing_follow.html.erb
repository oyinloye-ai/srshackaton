<%= @title %>
<div class="row">
	<div class="col-md-6">
		<h1><%= @user.email%></h1>
		<span><%= link_to "view my profile", @user%></span>
	</div>
	<div class="col-md-6">
		<% @title %>
		<% if @users.any? %>
			<%= render @users %>
	</div>
</div>